# Images for README

Drop the following files here and the main `README.md` will pick them up:

| File | What |
|---|---|
| `hero.png` | A 720px-wide screenshot of `skills-registry list` mid-fuzzy-filter — sells the TUI at a glance. |
| `sync.gif` | Short (~10s) recording of `skills-registry sync` multi-selecting + pushing skills. asciinema → gif is fine. |

Until these exist, the README shows broken-image icons with the alt text.
